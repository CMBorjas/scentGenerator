# Scent Generator API Configuration

# Port for the server to listen on
PORT=3001

# Optional API Key for authentication. 
# If set, clients must pass this via the 'x-api-key' header.
# If left blank or commented out, the API is public (though still rate-limited).
# SCENT_API_KEY=your_super_secret_key_here
